<!DOCTYPE html>
<html lang="en">
<head>
    <!-- The character encoding for the document is set to UTF-8 -->
    <meta charset="UTF-8">
    <!-- Ensures the page is responsive and adjusts to different screen sizes -->
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <!-- Title of the webpage, displayed on the browser tab -->
    <title>Computer Components Order Form 🖥️</title>
    <style>
        /* Basic styling for the entire page */
        body {
            font-family: Arial, sans-serif; /* Sets the font for the entire page */
            background: linear-gradient(to right, #f6c6c6, #f2e5f8); /* Light pink to lavender gradient background */
            margin: 0; /* Removes default margin around the body */
            padding: 0; /* Removes default padding around the body */
            display: flex; /* Uses flexbox layout to center content */
            justify-content: center; /* Centers content horizontally */
            align-items: center; /* Centers content vertically */
            height: 100vh; /* Makes the body take the full height of the viewport */
            color: #333; /* Sets a dark color for the text for better readability */
        }

        /* Styling for the form container */
        .order-form {
            background: #fff; /* White background for the form container */
            padding: 20px; /* Adds space inside the container */
            border-radius: 12px; /* Rounds the corners of the container */
            box-shadow: 0 8px 16px rgba(0, 0, 0, 0.2); /* Adds a subtle shadow to the container */
            width: 90%; /* Makes the form container responsive */
            max-width: 1000px; /* Sets a maximum width for larger screens */
            display: flex; /* Uses flexbox to layout the form sections horizontally */
            flex-direction: row; /* Aligns children horizontally */
            gap: 20px; /* Adds space between form sections */
            border: 2px solid #d6a4a4; /* Adds a border around the form container */
        }

        /* Styling for the form heading */
        .order-form h1 {
            text-align: center; /* Centers the heading text */
            font-size: 28px; /* Sets the font size of the heading */
            color: #f06c6c; /* Sets a pinkish color for the heading */
            margin-bottom: 20px; /* Adds space below the heading */
            font-weight: 700; /* Makes the heading bold */
        }

        /* Styling for each form section */
        .form-section {
            flex: 1; /* Allows each section to expand equally */
            padding: 15px; /* Adds space inside each section */
            border: 1px solid #f06c6c; /* Adds a border around each section */
            border-radius: 8px; /* Rounds the corners of each section */
            background-color: #fef5f5; /* Sets a light background color for each section */
        }

        /* Styling for the fieldset legend */
        .form-section legend {
            font-weight: bold; /* Makes the legend text bold */
            color: #f06c6c; /* Sets a pinkish color for the legend */
        }

        /* Styling for form labels */
        label {
            display: block; /* Makes labels block-level elements */
            margin-bottom: 8px; /* Adds space below each label */
            color: #555; /* Sets a dark gray color for labels */
            font-weight: bold; /* Makes labels bold */
        }

        /* Styling for form inputs, selects, and buttons */
        input, select, button {
            width: calc(100% - 24px); /* Sets the width to full minus padding */
            padding: 12px; /* Adds space inside the elements */
            margin-bottom: 15px; /* Adds space below each element */
            border: 2px solid #f06c6c; /* Adds a border color */
            border-radius: 6px; /* Rounds the corners of each element */
            font-size: 16px; /* Sets the font size for readability */
            box-sizing: border-box; /* Includes padding in the element's width */
            background-color: #fff; /* Sets a white background for inputs and selects */
        }

        /* Styling for focused inputs and selects */
        input:focus, select:focus {
            border-color: #f4a6a6; /* Changes the border color on focus */
            outline: none; /* Removes the default focus outline */
        }

        /* Styling for the submit button */
        button {
            background: #f06c6c; /* Sets a pinkish background color */
            color: #fff; /* Sets the text color to white */
            cursor: pointer; /* Changes the cursor to a pointer on hover */
            border: none; /* Removes the default border */
            font-size: 18px; /* Sets the font size of the button text */
            font-weight: bold; /* Makes the button text bold */
            margin-top: 30px; /* Adds space above the button */
            transition: background 0.3s ease; /* Smooth transition effect on hover */
        }

        /* Hover effect for the submit button */
        button:hover {
            background: #e55d5d; /* Darkens the button color on hover */
        }

        /* Styling for the order summary section */
        #orderSummary {
            display: none; /* Hides the order summary by default */
            text-align: center; /* Centers the text inside the summary */
            flex-basis: 100%; /* Takes full width of the container */
            border: 1px solid #f06c6c; /* Adds a border around the summary */
            border-radius: 8px; /* Rounds the corners of the summary */
            background-color: #fef5f5; /* Sets a light background color for the summary */
            padding: 20px; /* Adds space inside the summary */
            box-shadow: 0 8px 16px rgba(0, 0, 0, 0.2); /* Adds a subtle shadow to the summary */
        }

        /* Styling for the order summary heading */
        #orderSummary h2 {
            font-size: 24px; /* Sets the font size of the summary heading */
            margin-bottom: 15px; /* Adds space below the heading */
            color: #f06c6c; /* Sets a pinkish color for the heading */
        }

        /* Styling for the "Place Another Order" button */
        .small-button {
            font-size: 14px; /* Sets a smaller font size */
            padding: 6px 12px; /* Adds space inside the button */
            border-radius: 4px; /* Rounds the corners of the button */
            background-color: #f06c6c; /* Sets a pinkish background color */
            color: #fff; /* Sets the text color to white */
            cursor: pointer; /* Changes the cursor to a pointer on hover */
            border: none; /* Removes the default border */
            display: inline-block; /* Makes the button fit around the text */
            transition: background 0.3s ease; /* Smooth transition effect on hover */
        }

        /* Hover effect for the "Place Another Order" button */
        .small-button:hover {
            background: #e55d5d; /* Darkens the button color on hover */
        }

        /* Utility class to hide elements */
        .hidden {
            display: none; /* Hides elements */
        }
    </style>
</head>
<body>

    <!-- Container for the form and summary -->
    <div class="order-form" id="orderFormContainer">
        <!-- Section for the order form -->
        <div class="form-section">
            <h1>Order Your Computer Components 🖥️</h1>
            
            <!-- The form to order computer components -->
            <form id="componentForm">
                <!-- Section for entering customer information -->
                <fieldset>
                    <legend>Customer Information</legend>
                    <label for="name">Name:</label>
                    <input type="text" id="name" name="name" required>
                    
                    <label for="email">Email:</label>
                    <input type="email" id="email" name="email" required>
                    
                    <label for="phone">Phone Number:</label>
                    <input type="tel" id="phone" name="phone" required>
                </fieldset>
        </div>
        <div class="form-section">
                <!-- Section for selecting computer components -->
                <fieldset>
                    <legend>Choose Your Components</legend>
                    
                    <label for="processor">Processor:</label>
                    <select id="processor" name="processor">
                        <option value="i5">Intel i5</option>
                        <option value="i7">Intel i7</option>
                        <option value="ryzen5">AMD Ryzen 5</option>
                        <option value="ryzen7">AMD Ryzen 7</option>
                    </select>

                    <label for="ram">RAM:</label>
                    <select id="ram" name="ram">
                        <option value="8gb">8 GB</option>
                        <option value="16gb">16 GB</option>
                        <option value="32gb">32 GB</option>
                    </select>

                    <label for="storage">Storage:</label>
                    <select id="storage" name="storage">
                        <option value="256gb">256 GB SSD</option>
                        <option value="512gb">512 GB SSD</option>
                        <option value="1tb">1 TB SSD</option>
                    </select>

                    <label for="gpu">Graphics Card:</label>
                    <select id="gpu" name="gpu">
                        <option value="gtx1650">NVIDIA GTX 1650</option>
                        <option value="rtx3060">NVIDIA RTX 3060</option>
                        <option value="rtx3080">NVIDIA RTX 3080</option>
                    </select>
                </fieldset>
                <!-- Submit button for the form -->
                <button type="submit">Submit Order</button>
            </form>
        </div>

        <!-- Section for displaying the order summary -->
        <div id="orderSummary" class="hidden">
            <h2>Your Order Summary</h2>
            <p id="summaryText"></p>
            <!-- Button to place another order -->
            <button class="small-button" onclick="resetForm()">Place Another Order</button>
        </div>
    </div>

    <!-- JavaScript to handle form submission and reset -->
    <script>
        // Event listener for form submission
        document.getElementById('componentForm').addEventListener('submit', function(e) {
            e.preventDefault(); // Prevents the default form submission action

            // Retrieve the values entered by the user
            const name = document.getElementById('name').value;
            const email = document.getElementById('email').value;
            const phone = document.getElementById('phone').value;
            const processor = document.getElementById('processor').value;
            const ram = document.getElementById('ram').value;
            const storage = document.getElementById('storage').value;
            const gpu = document.getElementById('gpu').value;

            // Create a summary of the order
            const summaryText = `
                <strong>Name:</strong> ${name} <br>
                <strong>Email:</strong> ${email} <br>
                <strong>Phone:</strong> ${phone} <br>
                <strong>Processor:</strong> ${processor} <br>
                <strong>RAM:</strong> ${ram} <br>
                <strong>Storage:</strong> ${storage} <br>
                <strong>Graphics Card:</strong> ${gpu} <br>
            `;
            
            // Display the summary and hide the form
            document.getElementById('summaryText').innerHTML = summaryText;
            document.getElementById('orderFormContainer').style.display = 'none'; // Hides the form
            document.getElementById('orderSummary').style.display = 'block'; // Shows the summary
        });

        // Function to reset the form and show it again
        function resetForm() {
            document.getElementById('componentForm').reset(); // Resets form fields
            document.getElementById('orderFormContainer').style.display = 'flex'; // Shows the form
            document.getElementById('orderSummary').style.display = 'none'; // Hides the summary
        }
    </script>
</body>
</html>
